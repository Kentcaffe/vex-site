DATABASE_URL="file:./dev.db"
NEXT_PUBLIC_PRICE_CURRENCY=MDL
AUTH_SECRET="generate-with-openssl-rand-base64-32"

# OAuth (optional). În Google Cloud Console / Meta for Developers adaugi redirect URI:
#   http://localhost:3000/api/auth/callback/google
#   https://DOMENIUL-TAU/api/auth/callback/google
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# AUTH_FACEBOOK_ID=
# AUTH_FACEBOOK_SECRET=

# Real-time chat (Socket.io). In development run both via `npm run dev`
SOCKET_PORT=3001
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Render / producție ---
# Build Command: npm ci && npm run build:render   (rulează prisma db push + next build)
# Start Command: npm start
# DATABASE_URL: pe Render, folosește același path ca local sau un path pe Persistent Disk,
#   ex. file:./data/prod.db — altfel baza e efemeră la fiecare redeploy.
# AUTH_SECRET: obligatoriu (openssl rand -base64 32)
# NEXT_PUBLIC_APP_URL: https://vex.md (URL public al site-ului)
# NEXT_PUBLIC_SOCKET_URL: URL-ul serviciului Socket.io dacă îl rulezi separat (sau lasă gol dacă nu folosești chat în prod)
